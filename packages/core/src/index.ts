/**
 * @poolhall/core · 游戏逻辑层
 *
 * 职责（docs/hand-model.md）：局状态、规则、手感系统（hand model）、
 * trial 协议、双层视图（agentView / researchView）、行协议 schema。
 * 依赖方向：只可依赖 @poolhall/engine（AGENTS.md §1）。
 */

export const CORE_VERSION = "0.0.1";
